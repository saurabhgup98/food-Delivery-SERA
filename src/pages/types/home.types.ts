export interface FeatureDefinition {
    icon: string;
    text: string;
}

export interface StatDefinition {
    value: string;
    label: string;
    color: string;
}

export interface FloatingElementDefinition {
    emoji: string;
    position: string;
    size: string;
    bgColor: string;
    animationDelay: string;
}